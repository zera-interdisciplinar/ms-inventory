apiVersion: v1
kind: Service
metadata:
  name: ms-inventory
  namespace: qa
spec:
  selector:
    app: ms-inventory
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP